import { LandingPage } from "@/app/LandingPage";

// Oceanic Sunburst theme (palette2) — see .theme-oceanic in globals.css
export default function TemplateTwo() {
  return <LandingPage themeClass="theme-oceanic" />;
}
