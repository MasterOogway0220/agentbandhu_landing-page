import { LandingPage } from "@/app/LandingPage";

// Sunrise Glow theme (palette1) — see .theme-sunrise in globals.css
export default function TemplateOne() {
  return <LandingPage themeClass="theme-sunrise" />;
}
